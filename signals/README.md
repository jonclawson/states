# Analog / Angular Signals sample

Small Angular app demonstrating using Signals + Services to manage application state.

Features demonstrated:
- signal(), computed(), effect()
- Service-owned state (single source of truth)
- Derived state (computed)
- Simple persistence effect (localStorage)

Run:
  cd signals
  npm install
  npm run dev (app runs on port 4201)
