# Analog / NgRx sample

This is a minimal Angular app demonstrating NgRx core features:

- Store + reducers
- Actions & selectors
- Effects (async side-effects)
- Store DevTools integration

Run:

1. cd ngrx
2. npm install
3. npm run dev

Open http://localhost:4200
